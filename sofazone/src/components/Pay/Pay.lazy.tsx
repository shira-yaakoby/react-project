// import React, { lazy, Suspense } from 'react';

import Pay from "./Pay";

// const LazyPay = lazy(() => import('./Pay'));

// const Pay = (props: JSX.IntrinsicAttributes & { children?: React.ReactNode; }) => (
//   <Suspense fallback={null}>
//     <LazyPay {...props} />
//   </Suspense>
// );

export default Pay;
